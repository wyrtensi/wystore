# Copy to keystore.properties and fill in. That file is gitignored and must stay on your machine.
# Release builds are signed with this key; Wy Store refuses to update itself across a key change.
storeFile=outputs/WyStore-release-key.jks
storePassword=
keyAlias=
keyPassword=
